# schemas

This directory is reserved for exported JSON Schema files and schema documentation.

The source of truth is the Pydantic implementation in `src/video_truthfulness/schemas.py`.

When schema exports are added later, keep them generated from code instead of hand-editing them.
