# runs

Runtime outputs are written here during local execution.

Only this README should be committed. Real run directories are ignored by Git.

Expected local structure:

```text
runs/<run_id>/
  report.md
  report.json
  evidence_manifest.json
  screenshots/
  media/
  frames/
```
